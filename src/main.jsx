import React from 'react'
import ReactDOM from 'react-dom/client'
import SyncGen from './SyncGen.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SyncGen />
  </React.StrictMode>,
)
